[project]
name = "small-repo"
version = "0.1.0"
description = "A small Python project with Port.io GitOps integration utilities"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.2",
]
